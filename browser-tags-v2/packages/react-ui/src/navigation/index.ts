import { nextRouterNavigation } from "./next-router";

/**
 * Helper function to navigate to a new page depending on the navigation library used.
 */
export const Navigation = {
  nextRouterNavigation,
};
